<?php
/**
 * Copyright © Frodo. All rights reserved.
 *
 * @var \Frodo\Antifraud\Block\Adminhtml\GenericAddForm $block
 */
?>
<form action="<?= $block->escapeUrl($block->getSaveUrl()) ?>" method="post" id="frodo_antifraud_add_form">
    <?= $block->getBlockHtml('formkey') ?>
    <fieldset class="admin__fieldset">
        <?php foreach ($block->getFields() as $fieldName => $fieldConfig): ?>
            <div class="admin__field<?= !empty($fieldConfig['required']) ? ' _required' : '' ?>">
                <label class="admin__field-label" for="<?= $block->escapeHtmlAttr($fieldName) ?>">
                    <span><?= $block->escapeHtml(__($fieldConfig['label'] ?? $fieldName)) ?></span>
                </label>
                <div class="admin__field-control">
                    <input type="<?= $block->escapeHtmlAttr($fieldConfig['type'] ?? 'text') ?>"
                           name="<?= $block->escapeHtmlAttr($fieldName) ?>"
                           id="<?= $block->escapeHtmlAttr($fieldName) ?>"
                           class="admin__control-text"
                           <?= !empty($fieldConfig['required']) ? 'required' : '' ?>/>
                    <?php if (!empty($fieldConfig['note'])): ?>
                        <div class="admin__field-note">
                            <span><?= $block->escapeHtml(__($fieldConfig['note'])) ?></span>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        <?php endforeach; ?>
    </fieldset>
    <div class="admin__page-actions">
        <button type="button" class="action-default scalable back" onclick="location.href='<?= $block->escapeUrl($block->getBackUrl()) ?>'">
            <span><?= $block->escapeHtml(__('Back')) ?></span>
        </button>
        <button type="submit" class="action-default scalable save primary">
            <span><?= $block->escapeHtml(__('Save')) ?></span>
        </button>
    </div>
</form>
